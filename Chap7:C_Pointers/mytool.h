#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void print2DArray(unsigned int *ar, int row, int col)
{
    for (size_t i = 0; i < row; i++)
    {
        for (size_t j = 0; j < col; j++)
        {
            // printf("%d", ar[i][j]);
        }
    }
}